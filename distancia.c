
#include <stdio.h>
#include <math.h>

float calculaDistancia(int x1, int y1, int x2,int y2){
return sqrt (pow (x2-x1,2)+pow(y2-y1,2));	
}
int main (){
	int x1, y1, x2, y2;
	float distancia;
	printf("Coordenadas primmeiro ponto : ");
	scanf("%i %i", &x1, &y1);
	printf("Coordenadas segundo ponto : ");
	scanf("%i %i", &x2, &y2);
}

#include <stdio.h>
#include <math.h>

float calculaDistancia(int x1, int y1, int x2, int y2){
return sqrt (pow (x2-x1,2)+pow(y2-y1,2));	
}
int main (){
	int x1, y1, x2, y2;
	float distancia;
	printf("Coordenadas primmeiro ponto : ");
	scanf("%i %i", &x1, &y1);
	printf("Coordenadas segundo ponto : ");
	scanf("%i %i", &x2, &y2);
	distancia = calculaDistancia(x1, y1,x2,y2);
	printf("Distancia : %.2f", distancia);
}
}
#include <stdio.h>
#include <math.h>

float calculaDistancia(int x1, int y1, int x2 int y2){
return sqrt (pow (x2-x1,2)+pow(y2-y1,2));	
}
int main (){
	
}
#include <stdio.h>
#include <math.h>

float calculaDistancia(int x1, int y1, int x2, int y2){
return sqrt (pow (x2-x1,2)+pow(y2-y1,2));	
}

int main()
{
	int x1, y1, x2, y2;
	float distancia;
	printf("Primeiro ponto Coordenadas: ");
	scanf("%d %d", &x1, &y1);
	printf("Segundo ponto Coordenadas: ");
	scanf("%d %d", &x2, &y2);
	distancia = calculaDistancia(x1,y1,x2,y2);
	printf("Distancia : %f", distancia);

}
#include <stdio.h>
#include <math.h>

float calculaDistancia(int x1, int y1, int x2 int y2){
return sqrt (pow (x2-x1,2)+pow(y2-y1,2));	
}
int main (){
	int x1, y1, x2, y2;
	float distancia;
	printf("Coordenadas primmeiro ponto : ");
	scanf("%i %i", &x1, &y1);
	printf("Coordenadas segundo ponto : ");
	scanf("%i %i", &x2, &y2)
}